作業七 Canvas video
(1)透過截取video中的影像，將每一張影像使用requestAnimationFrame()將影像畫至Canvas V
(2)release 時，使用cancelAnimationFrame取消操作，並設定load為false，使
(3)create  時，使用requestAnimationFrame，並設定load為true 問題是(畫面會閃爍之後才繼續畫截圖)
(4)zoom in 
(5)zoom out


requestID = window.requestAnimationFrame(callback);       // Firefox 23 / IE 10 / Chrome
requestID = window.mozRequestAnimationFrame(callback);    // Firefox < 23
requestID = window.webkitRequestAnimationFrame(callback); // Safari
